@model IEnumerable<Category>
<li class="nav-item dropdown">
    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        Teas
    </a>
    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
        @foreach (var category in Model)
        {
            <a class="dropdown-item" asp-controller="Tea" asp-action="List" asp-route-category="@category.CategoryName">@category.CategoryName</a>
        }
        <div class="dropdown-divider"></div>
        <a class="dropdown-item" asp-controller="Tea" asp-action="List" asp-route-Category="">View all teas</a>
    </div>
</li>

        @*<li class="dropdown">
                <a asp-controller="Tea"
                   asp-action="Index"
                   class="dropdown-toggle" data-toggle="dropdown">Teas<b class="caret"></b></a>
                <ul class="dropdown-menu">
                    @foreach (var category in Model)
                    {
                        <li>
                            <a asp-controller="Tea" asp-action="List"
                               asp-route-category="@category.CategoryName">@category.CategoryName</a>
                        </li>
                    }
                    <li class="divider"></li>
                    <li>
                        <a asp-controller="Tea" asp-action="List" asp-route-Category="">View all teas</a>
                    </li>
                </ul>
            </li>*@
